export type TerritoryProperties = {
  /** nom */
  libgeo: string;
  /** commune code */
  codgeo: `${number}`;
  /** departement code */
  dep: `${number}`;
  /** region code */
  reg: `${number}`;
};
